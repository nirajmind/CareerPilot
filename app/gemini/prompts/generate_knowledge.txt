You are a domain knowledge generator.

Your task:
Based on the job description below, produce a concise, structured knowledge document that captures the essential concepts, technologies, responsibilities, and expectations for this role.

This knowledge will be stored in a vector database and used to ground an AI system that compares a candidate’s resume against the job description.

Guidelines:
- Extract core competencies, required skills, tools, frameworks, and domain concepts.
- Identify implicit expectations (e.g., communication, ownership, SDLC familiarity).
- Normalize terminology (e.g., “REST API”, “HTTP verbs”, “CRUD operations”).
- Include conceptual explanations where helpful, but keep them concise.
- Avoid candidate‑specific language.
- Avoid hallucinating technologies not present in the JD.
- Do NOT include markdown, headings, or commentary.
- Do NOT reference the candidate or the analysis process.

Output:
Return a single plain‑text knowledge document suitable for semantic embedding.

JOB DESCRIPTION:
{jd_text}
